# The following secrets are not real secrets and were included only for debugging purposes
# Remember to replace them with actual secrets in production and to keep them secure

# Application
APP_SECRET_KEY=s3cret_k3y
APP_HOST=localhost
APP_PORT=8000
APP_DEBUG=True
