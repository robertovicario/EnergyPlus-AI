services:

    app:
        container_name: "app"
        build:
            context: .
            dockerfile: Dockerfile
        env_file:
            - env/app.env
        environment:
            - PORT=8000
        ports:
            - "8000:8000"
        restart: unless-stopped
